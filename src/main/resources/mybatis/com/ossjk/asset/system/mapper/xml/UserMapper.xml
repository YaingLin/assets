<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ossjk.asset.system.mapper.UserMapper">

	<!-- 通用查询映射结果 -->
	<resultMap id="BaseResultMap"
		type="com.ossjk.asset.system.entity.User">
		<id column="id" property="id" />
		<result column="rid" property="rid" />
		<result column="ogid" property="ogid" />
		<result column="name" property="name" />
		<result column="pwd" property="pwd" />
		<result column="sex" property="sex" />
		<result column="birth" property="birth" />
		<result column="phone" property="phone" />
		<result column="email" property="email" />
		<result column="logintime" property="logintime" />
		<result column="loginip" property="loginip" />
		<result column="crtm" property="crtm" />
		<result column="mdtm" property="mdtm" />
	</resultMap>
	
	<select id="selectUserVo"
		resultType="com.ossjk.asset.system.vo.UserVo">
		SELECT
			r.NAME rname,
			o.NAME oname,
			u.*
		FROM
			user u,
			role r,
			organization o
		WHERE
			u.ogid = o.id
			AND u.rid = r.id
		${ew.sqlSegment}
	</select>
	
</mapper>
